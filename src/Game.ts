import { Game, Types } from "phaser";
import { BaseGameScene } from "./Scene/BaseGameScene";

export class explorerGame extends Game{
    constructor(GameConfig?: Types.Core.GameConfig){
        super(GameConfig);
    }
}